import { useEffect, useMemo, useState } from "react";
import { Card, CardContent, CardHeader } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { ArrowLeft, ArrowRight, Check, ChevronDown, ChevronUp, Volume2, X } from "lucide-react";

type Example = { jp?: string; id?: string; reading?: string; note?: string };
type RelatedWord = { term: string; reading?: string | null; meaning?: string | null; example?: string | null };
type RelatedKanji = { character: string; meaning?: string | null; level?: string | null; reading?: string | null };
type Props = { index:number; total:number; level:string; title:string; reading?:string|null; romaji?:string|null; meaning:string; secondary?:string|null; structure?:string|null; explanation?:string|null; examples?:Example[]; relatedWords?:RelatedWord[]; relatedKanji?:RelatedKanji[]; onyomi?:string[]; kunyomi?:string[]; usageNotes?:string[]; commonMistakes?:string[]; question?:{prompt:string;choices:string[];correctIndex:number;explanation?:string}|null; furiganaEnabled?:boolean; kotobaMode?:boolean; kanjiMode?:boolean; radical?:string|null; onPrev:()=>void; onNext:()=>void; onLearned?:()=>void; learned?:boolean };
const sectionTitle = "text-xs font-bold tracking-wide text-muted-foreground";
export function StudyFlashcard({index,total,level,title,reading,romaji,meaning,secondary,explanation,examples,relatedWords,relatedKanji,onyomi,kunyomi,usageNotes,commonMistakes,question,furiganaEnabled=true,kotobaMode=false,kanjiMode=false,radical,onPrev,onNext,onLearned,learned}:Props){const[showDetails,setShowDetails]=useState(true);const progress=total?Math.round(((index+1)/total)*100):0;const usableExamples=useMemo(()=> (examples??[]).filter(x=>x.jp||x.id).slice(0,6),[examples]);useEffect(()=>{setShowDetails(true);window.speechSynthesis?.cancel()},[index,title]);const speak=(text:string)=>{if(!window.speechSynthesis)return;window.speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.lang="ja-JP";u.rate=.85;window.speechSynthesis.speak(u)};return <div className="mx-auto max-w-2xl"><div className="mb-3 flex items-center justify-between text-xs text-muted-foreground"><span>Materi {index+1} dari {total}</span><span>{progress}% selesai</span></div><div className="mb-5 h-1.5 overflow-hidden rounded-full bg-muted"><div className="h-full rounded-full bg-primary transition-all" style={{width:`${progress}%`}}/></div><Card className="overflow-hidden border-border/80 shadow-xl"><CardHeader className="space-y-4 bg-gradient-to-br from-primary/10 via-background to-secondary/20 px-6 py-8 text-center sm:px-8"><div className="flex items-center justify-center gap-2"><Badge variant="secondary">{level}</Badge><Badge variant="outline">{kanjiMode?"KANJI":kotobaMode?"KOTOBA":"Pelajaran"}</Badge></div>{kanjiMode?<div lang="ja" className="font-jp text-8xl font-bold leading-none tracking-wide sm:text-9xl">{furiganaEnabled&&reading?<ruby>{title}<rt className="mb-2 text-xl font-normal text-muted-foreground sm:text-2xl">{reading}</rt></ruby>:title}</div>:<div lang="ja" className="font-jp text-5xl font-bold tracking-wide sm:text-6xl">{title}{kotobaMode&&reading&&<span className="ml-3 text-2xl font-normal text-muted-foreground sm:text-3xl">— {reading}</span>}</div>}{!kotobaMode&&!kanjiMode&&reading&&furiganaEnabled&&<div lang="ja" className="text-base text-muted-foreground">{reading}</div>}{kotobaMode&&romaji&&<div className="text-base italic text-muted-foreground">{romaji}</div>}<div className="text-xl font-semibold leading-relaxed"><span className="text-sm font-medium text-muted-foreground">Arti :</span><div className="mt-1">{meaning}</div></div>{!kanjiMode&&secondary&&<p className="text-sm text-muted-foreground">{secondary}</p>}<div className="flex flex-wrap justify-center gap-2"><Button type="button" variant="outline" size="sm" onClick={()=>speak(title)}><Volume2 className="mr-2 size-4"/><span>Dengarkan</span></Button><Button type="button" variant="ghost" size="sm" onClick={()=>setShowDetails(v=>!v)}>{showDetails?<ChevronUp className="mr-2 size-4"/>:<ChevronDown className="mr-2 size-4"/>}<span>{showDetails?"Sembunyikan penjelasan":"Tampilkan penjelasan lengkap"}</span></Button></div></CardHeader><CardContent className="space-y-6 p-5 sm:p-7">{showDetails&&<div className="space-y-6">{kanjiMode?<><section className="rounded-2xl border bg-muted/30 p-5"><p className={sectionTitle}>KUNYOMI</p><p lang="ja" className="mt-3 font-jp text-xl leading-8">{kunyomi?.length?kunyomi.join("・"):"—"}</p></section><section className="rounded-2xl border bg-muted/30 p-5"><p className={sectionTitle}>ONYOMI</p><p lang="ja" className="mt-3 font-jp text-xl leading-8">{onyomi?.length?onyomi.join("・"):"—"}</p></section><section className="rounded-2xl border bg-muted/30 p-5"><p className={sectionTitle}>KANJI TERKAIT / RADIKAL</p>{radical&&<div className="mt-3 rounded-xl border bg-background p-4"><p className="text-xs text-muted-foreground">Radikal</p><p lang="ja" className="font-jp text-3xl">{radical}</p></div>}{relatedKanji?.length?<div className="mt-4 grid gap-3 sm:grid-cols-2">{relatedKanji.slice(0,10).map((k,i)=><div key={`${k.character}-${i}`} className="rounded-xl border bg-background p-4"><p lang="ja" className="font-jp text-3xl">{k.reading&&furiganaEnabled?<ruby>{k.character}<rt className="text-xs font-normal text-muted-foreground">{k.reading}</rt></ruby>:k.character}</p><p className="mt-2 text-sm">{k.meaning||"Arti belum tersedia"}</p>{k.level&&<Badge variant="outline" className="mt-2">{k.level}</Badge>}</div>)}</div>:!radical&&<p className="mt-3 text-sm text-muted-foreground">Belum ada data kanji terkait.</p>}</section><section className="rounded-2xl border bg-muted/30 p-5"><p className={sectionTitle}>CONTOH KANJI TERKAIT</p>{usableExamples.length?<div className="mt-4 space-y-3">{usableExamples.map((example,i)=><div key={`${example.jp??"contoh"}-${i}`} className="rounded-xl border bg-background p-4"><div className="flex items-start gap-2"><div className="flex-1">{example.jp&&<p lang="ja" className="font-jp text-lg leading-8">{example.jp}</p>}{example.reading&&furiganaEnabled&&<p lang="ja" className="mt-1 text-sm text-muted-foreground">{example.reading}</p>}{example.id&&<p className="mt-2 text-sm leading-6">{example.id}</p>}</div>{example.jp&&<Button type="button" size="icon" variant="ghost" aria-label="Dengarkan contoh" onClick={()=>speak(example.jp!)}><Volume2 className="size-4"/></Button>}</div></div>)}</div>:<p className="mt-3 text-sm text-muted-foreground">Belum ada contoh kanji terkait.</p>}</section></>:<>{explanation&&<section className="rounded-2xl border bg-muted/30 p-5"><p className={sectionTitle}>PENJELASAN</p><p className="mt-3 whitespace-pre-line leading-7">{explanation}</p></section>}{!!usageNotes?.length&&<section className="rounded-2xl border bg-muted/30 p-5"><p className={sectionTitle}>COCOK DIGUNAKAN DALAM KONTEKS</p><ul className="mt-3 list-disc space-y-2 pl-5 leading-7">{usageNotes.map((n,i)=><li key={`${n}-${i}`}>{n}</li>)}</ul></section>}{!!usableExamples.length&&<section className="rounded-2xl border bg-muted/30 p-5"><p className={sectionTitle}>CONTOH KALIMAT</p><div className="mt-4 space-y-4">{usableExamples.map((example,i)=><div key={`${example.jp??"contoh"}-${i}`} className="rounded-xl border bg-background p-4"><div className="flex items-start gap-2"><div className="flex-1">{example.jp&&<p lang="ja" className="font-jp text-lg leading-8">{example.jp}</p>}{example.reading&&furiganaEnabled&&<p lang="ja" className="mt-1 text-sm text-muted-foreground">{example.reading}</p>}{example.id&&<p className="mt-2 text-sm leading-6">{example.id}</p>}{example.note&&<p className="mt-2 text-xs leading-5 text-muted-foreground">Catatan: {example.note}</p>}</div>{example.jp&&<Button type="button" size="icon" variant="ghost" aria-label="Dengarkan contoh kalimat" onClick={()=>speak(example.jp!)}><Volume2 className="size-4"/></Button>}</div></div>)}</div></section>}{!!commonMistakes?.length&&<section className="rounded-2xl border border-destructive/20 bg-destructive/5 p-5"><p className="text-xs font-bold tracking-wide text-destructive">KESALAHAN YANG SERING TERJADI</p><div className="mt-3 space-y-3">{commonMistakes.map((n,i)=><div key={`${n}-${i}`} className="rounded-xl bg-background p-4 leading-7">{n}</div>)}</div></section>}{!!relatedWords?.length&&<section className="rounded-2xl border bg-muted/30 p-5"><p className={sectionTitle}>KOSAKATA TERKAIT</p><div className="mt-4 grid gap-3 sm:grid-cols-2">{relatedWords.slice(0,10).map((w,i)=><div key={`${w.term}-${i}`} className="rounded-xl border bg-background p-4"><p lang="ja" className="font-jp text-xl">{w.term}</p>{furiganaEnabled&&w.reading&&<p lang="ja" className="mt-1 text-sm text-muted-foreground">{w.reading}</p>}<p className="mt-2 text-sm leading-6">{w.meaning||"Arti belum tersedia"}</p>{w.example&&<p lang="ja" className="mt-2 border-t pt-2 font-jp text-sm leading-6">{w.example}</p>}</div>)}</div></section>}{question&&<section className="rounded-2xl border-2 border-primary/20 bg-primary/5 p-5"><p className="text-xs font-bold tracking-wide text-primary">LATIHAN PEMAHAMAN</p><p className="mt-3 font-medium leading-7">{question.prompt}</p><div className="mt-4 grid gap-3">{question.choices.map((choice,i)=><Button key={`${choice}-${i}`} type="button" variant="outline" className="h-auto min-h-12 justify-start whitespace-normal py-3 text-left">{String.fromCharCode(65+i)}. {choice}</Button>)}</div></section>}</>}</div>}{onLearned&&<div className="mt-1"><Button type="button" variant={learned?"secondary":"outline"} onClick={onLearned} disabled={!!learned} className="h-11 w-full rounded-xl font-semibold">{learned?<><Check className="mr-2 size-4"/><span>Sudah dipelajari</span></>:<span>Tandai sudah dipelajari</span>}</Button></div>}<div className="rounded-2xl border bg-muted/30 p-2 sm:p-2.5"><div className="grid grid-cols-2 gap-2"><Button type="button" variant="ghost" onClick={onPrev} disabled={index===0} className="h-12"><ArrowLeft className="mr-2 size-4"/><span>Sebelumnya</span></Button><Button type="button" onClick={onNext} disabled={index===total-1} className="h-12"><span>Berikutnya</span><ArrowRight className="ml-2 size-4"/></Button></div></div></CardContent></Card></div>}
