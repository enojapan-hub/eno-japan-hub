import { createFileRoute, Link } from "@tanstack/react-router";import { useQuery } from "@tanstack/react-query";import { AppShell } from "@/components/layout/AppShell";import { Card,CardContent,CardDescription,CardHeader,CardTitle } from "@/components/ui/card";import { Badge } from "@/components/ui/badge";import { supabase } from "@/integrations/supabase/client";import { fetchTargetLevel } from "@/lib/target-level";import type { Level } from "@/lib/learn-queries";
export const Route=createFileRoute("/_authenticated/belajar")({head:()=>({meta:[{title:"Belajar — ENO JAPAN"},{name:"description",content:"Pusat belajar sesuai level JLPT pilihanmu."}]}),component:BelajarPage});
const sections=[{to:"/kanji",jp:"漢字",label:"Kanji",desc:"Flashcard kanji, bacaan, arti, audio, contoh, dan latihan.",table:"kanji"},{to:"/kotoba",jp:"言葉",label:"Kotoba",desc:"Kosakata dengan bacaan, arti, contoh kalimat, audio, dan latihan.",table:"vocabulary"},{to:"/bunpo",jp:"文法",label:"Bunpō",desc:"Tata bahasa dengan pola, penjelasan, contoh, kesalahan umum, dan latihan.",table:"grammar_points"},{to:"/dokkai",jp:"読解",label:"Dokkai",desc:"Bacaan Jepang sesuai level dengan pertanyaan dan pembahasan.",table:"reading_passages"},{to:"/listening",jp:"聴解",label:"Chōkai",desc:"Latihan menyimak sesuai level dengan audio, soal, dan pembahasan.",table:"listening_items"}] as const;
async function fetchLevelCounts(level:Level){const results=await Promise.all(sections.map(s=>supabase.from(s.table).select("id",{count:"exact",head:true}).eq("level",level).eq("is_published",true)));return Object.fromEntries(sections.map((s,i)=>[s.table,results[i].count??0]));}
function BelajarPage(){const{data:level,isLoading:levelLoading}=useQuery({queryKey:["target-level"],queryFn:fetchTargetLevel});const selected:Level=level??"N5";const{data:counts,isLoading}=useQuery({queryKey:["content-counts",selected],queryFn:()=>fetchLevelCounts(selected),enabled:!!level});return <AppShell title="Belajar" description={`Materi yang ditampilkan hanya untuk level ${selected} yang kamu pilih di profil.`} backTo="/dashboard" backLabel="Beranda"><div className="mb-5 rounded-2xl border bg-gradient-to-r from-primary/10 via-background to-secondary/20 p-5"><p className="text-sm font-semibold">Level belajar: <span className="text-primary">{levelLoading?"Memuat…":selected}</span></p><p className="mt-1 text-sm text-muted-foreground">ENO JAPAN tidak memuat seluruh N5–N1 sekaligus. Pilih level di Profil untuk mengganti materi.</p></div><div className="grid gap-4 sm:grid-cols-2">{sections.map(s=><Link key={s.to} to={s.to} className="group block focus:outline-none"><Card className="h-full transition-all duration-200 group-hover:-translate-y-0.5 group-hover:border-primary/50 group-hover:shadow-lg group-focus-visible:ring-2 group-focus-visible:ring-primary"><CardHeader className="flex flex-row items-start justify-between gap-4"><div><span lang="ja" className="font-jp text-3xl text-primary">{s.jp}</span><CardTitle className="mt-2 text-base">{s.label}</CardTitle><CardDescription className="mt-1 leading-6">{s.desc}</CardDescription></div><Badge variant="secondary">{isLoading||levelLoading?"…":`${counts?.[s.table]??0} materi`}</Badge></CardHeader><CardContent className="text-xs text-muted-foreground">Klik untuk mulai belajar →</CardContent></Card></Link>)}</div></AppShell>}
